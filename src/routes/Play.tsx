import { cn } from '@/utils';

export const Play = () => {
  return (
    <div
      className={cn(
        'flex h-screen flex-col items-center justify-center gap-20',
        'bg-red-100',
      )}
    >
      Play
    </div>
  );
};
